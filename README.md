# MCP UI Todo App

Simple Todo app using MCP UI protocol.

- **Server:** Express + MCP UI
- **Client:** React + @mcp-ui/client

## Run

1. Install dependencies:

```bash
npm install

Start server:
cd server
npm run dev

Start client:
cd client
npm start

